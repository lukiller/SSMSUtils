<dw:DialogWindow 
    x:Class="LKZ.SSMSUtils.Views.MacrossView"
    xmlns:dw="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.15.0"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    WindowStartupLocation="CenterOwner"
    x:Name="aboutDialog"
    Loaded="Window_Loaded" 
    SizeToContent="WidthAndHeight" 
    ResizeMode="NoResize" 
    mc:Ignorable="d" 
    Title="Macross" 
    d:DesignHeight="600" d:DesignWidth="400">
    
    <Grid ClipToBounds="False">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center">LKZ</TextBlock>

        <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Right">
            <dw:DialogButton Name="OKButton" IsCancel="True" IsDefault="True" Click="OKButton_Click">OK</dw:DialogButton>
        </StackPanel>
    </Grid>
</dw:DialogWindow>
